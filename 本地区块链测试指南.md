# 本地区块链测试指南

## 概述

本指南介绍如何在本地运行区块链节点进行开发和测试，避免使用真实网络和花费真实的Gas费用。

## 主要工具对比

### 1. Anvil (推荐)
- **优势**: 性能最佳，Rust编写，Foundry生态
- **适用**: 高性能测试，复杂场景
- **安装**: `curl -L https://foundry.paradigm.xyz | bash && foundryup`

### 2. Hardhat Network
- **优势**: JavaScript生态，调试功能强大
- **适用**: 前端开发，智能合约开发
- **安装**: `npm install --save-dev hardhat`

### 3. Ganache
- **优势**: 图形界面，易于使用
- **适用**: 初学者，可视化需求
- **安装**: 下载GUI版本或 `npm install -g ganache-cli`

## 快速开始 - 使用 Anvil

### 1. 安装 Foundry
```bash
# 方法1: 官方安装脚本 (可能较慢)
curl -L https://foundry.paradigm.xyz | bash
foundryup

# 方法2: 使用 Homebrew (推荐，更快)
brew install foundry
```

### 2. 启动本地节点
```bash
# 启动 Anvil (默认端口 8545)
anvil

# 自定义配置启动
anvil --port 8545 --accounts 10 --balance 1000
```

### 3. 配置项目使用本地网络
```bash
# 复制环境变量文件
cp .env.example .env

# 编辑 .env 文件，设置为本地网络
NETWORK_TYPE=local
ETHEREUM_RPC=http://127.0.0.1:8545
ETHEREUM_WS_RPC=ws://127.0.0.1:8545
```

### 4. 运行你的Go程序
```bash
go run main.go
```

## 快速开发测试

### 1. 基本测试流程
```bash
# 终端1: 启动本地节点
anvil

# 终端2: 运行测试示例
go run examples/08_local_testing.go
```

### 2. 测试结果示例
```
=== 本地区块链测试示例 ===
连接成功! Chain ID: 31337

=== 账户余额 ===
账户 0 (0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266): 9998.999958 ETH
账户 1 (0x70997970C51812dc3A010C7d01b50e0d17dc79C8): 10001 ETH

=== 发送测试交易 ===
交易已发送! Hash: 0xc9b621ce...
从 0xf39Fd6e... 向 0x70997970... 发送 1 ETH

=== 最新区块信息 ===
最新区块号: 1
区块哈希: 0x189a46be...
交易数量: 1
Gas使用量: 21000
```

### 3. 开发优势
- ✅ **即时确认**: 交易立即被挖掘到区块
- ✅ **无Gas费用**: 使用免费的测试ETH
- ✅ **完全控制**: 可以重置、快照、时间操作
- ✅ **快速迭代**: 无需等待网络确认

### 4. 常用开发命令
```bash
# 方法1: 使用 cast 命令 (推荐)
cast balance 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266 --rpc-url http://127.0.0.1:8545
cast block latest --rpc-url http://127.0.0.1:8545

# 方法2: 直接调用 JSON-RPC API
curl -X POST -H "Content-Type: application/json" \
  -d '{"jsonrpc": "2.0", "method": "eth_getBalance", "params": ["0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266", "latest"], "id": 1}' \
  http://127.0.0.1:8545

# 方法3: 使用我们的Go示例
go run examples/09_rpc_calls.go

# 方法4: 使用脚本工具
./scripts/check_balance.sh
./scripts/check_balance.sh 0x70997970C51812dc3A010C7d01b50e0d17dc79C8
```

### 5. JSON-RPC API 调用示例

#### 查询链ID
```bash
curl -X POST -H "Content-Type: application/json" \
  -d '{"jsonrpc": "2.0", "method": "eth_chainId", "params": [], "id": 1}' \
  http://127.0.0.1:8545
# 返回: {"jsonrpc":"2.0","id":1,"result":"0x7a69"}  # 0x7a69 = 31337
```

#### 查询账户余额
```bash
curl -X POST -H "Content-Type: application/json" \
  -d '{"jsonrpc": "2.0", "method": "eth_getBalance", "params": ["0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266", "latest"], "id": 1}' \
  http://127.0.0.1:8545
# 返回: {"jsonrpc":"2.0","id":1,"result":"0x21df03e39e1dd38c2b8"}  # Wei格式
```

#### 查询最新区块号
```bash
curl -X POST -H "Content-Type: application/json" \
  -d '{"jsonrpc": "2.0", "method": "eth_blockNumber", "params": [], "id": 1}' \
  http://127.0.0.1:8545
```

#### 查询Gas价格
```bash
curl -X POST -H "Content-Type: application/json" \
  -d '{"jsonrpc": "2.0", "method": "eth_gasPrice", "params": [], "id": 1}' \
  http://127.0.0.1:8545
```

### 6. 数值转换
- **16进制转10进制**: `0x7a69` = `31337`
- **Wei转ETH**: `0x21df03e39e1dd38c2b8` ≈ `9998.999958 ETH`
- **在线转换工具**: https://eth-converter.com/

## 使用 Hardhat Network

### 1. 安装 Hardhat
```bash
npm init -y
npm install --save-dev hardhat
```

### 2. 启动节点
```bash
npx hardhat node
```

### 3. 配置连接
- RPC: `http://127.0.0.1:8545`
- Chain ID: `31337`

## 使用 Ganache

### 1. 安装
```bash
# CLI 版本
npm install -g ganache-cli

# 或下载 GUI 版本
# https://trufflesuite.com/ganache/
```

### 2. 启动
```bash
# CLI 启动
ganache-cli --port 8545 --accounts 10 --deterministic

# GUI 版本直接启动应用
```

## 本地测试的优势

### 1. 开发效率
- **快速交易确认**: 无需等待区块确认
- **无Gas费用**: 使用测试ETH，无真实成本
- **可重置状态**: 随时重启获得干净环境

### 2. 调试便利
- **详细日志**: 查看所有交易和事件
- **时间控制**: 可以手动推进区块时间
- **状态检查**: 随时查看账户余额和合约状态

### 3. 测试场景
- **极端情况**: 模拟网络拥堵、高Gas价格
- **错误处理**: 测试交易失败、重组等情况
- **性能测试**: 大量交易的处理能力

## 常用测试账户

Anvil 默认提供的测试账户：
```
Account #0: 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266
Private Key: 0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

Account #1: 0x70997970C51812dc3A010C7d01b50e0d17dc79C8
Private Key: 0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d
```

## 部署测试合约

### 1. 简单 ERC20 合约
```solidity
// TestToken.sol
pragma solidity ^0.8.0;

import "@openzeppelin/contracts/token/ERC20/ERC20.sol";

contract TestToken is ERC20 {
    constructor() ERC20("Test Token", "TEST") {
        _mint(msg.sender, 1000000 * 10**decimals());
    }
}
```

### 2. 使用 Forge 部署
```bash
# 编译合约
forge build

# 部署到本地网络
forge create --rpc-url http://127.0.0.1:8545 \
  --private-key 0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80 \
  src/TestToken.sol:TestToken
```

## 网络配置切换

### 环境变量配置
```bash
# 本地测试
export NETWORK_TYPE=local

# 测试网
export NETWORK_TYPE=testnet

# 主网
export NETWORK_TYPE=mainnet
```

### 程序中的使用
```go
config := config.LoadConfig()
fmt.Printf("连接到 %s 网络: %s\n", config.NetworkType, config.EthereumRPC)
```

## 常见问题

### 1. 连接失败
- 检查节点是否启动
- 确认端口号正确 (默认8545)
- 检查防火墙设置

### 2. 交易失败
- 检查账户余额是否足够
- 确认Gas限制设置
- 查看节点日志获取详细错误

### 3. 合约交互问题
- 确认合约地址正确
- 检查ABI是否匹配
- 验证函数参数类型

## 最佳实践

1. **使用确定性账户**: 便于测试重现
2. **定期重置状态**: 避免测试数据污染
3. **模拟真实场景**: 包括网络延迟、Gas价格波动
4. **自动化测试**: 编写脚本自动部署和测试
5. **监控资源使用**: 避免本地节点占用过多资源

## 进阶功能

### Fork 主网
```bash
# Fork 主网到本地进行测试
anvil --fork-url https://mainnet.infura.io/v3/YOUR_API_KEY
```

### 时间操作
```bash
# 推进区块时间
cast rpc evm_increaseTime 3600  # 增加1小时
cast rpc evm_mine              # 挖掘新区块
```

### 快照和恢复
```bash
# 创建快照
cast rpc evm_snapshot

# 恢复到快照
cast rpc evm_revert 0x1  # 快照ID
```

这样你就可以在本地进行完整的区块链开发和测试了！

