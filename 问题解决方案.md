# 网络连接问题解决方案

## 🎉 问题已解决！

你遇到的 `connection reset by peer` 错误已经通过以下方式解决：

### 原因分析

**网络连接不稳定**导致：
1. Infura 节点偶尔会有连接重置
2. 你的网络环境可能有防火墙限制
3. 单个节点不够可靠

###  解决方案

#### ✅ 方案 1: 使用公共免费节点（无需 API Key）

我们升级了程序，现在使用多个备用节点：

```go
nodes := []string{
    "https://eth.llamarpc.com",      // LlamaNodes 公共节点 ⭐推荐
    "https://cloudflare-eth.com",    // Cloudflare 节点
    "https://mainnet.infura.io/v3/...", // 你的 Infura
    "https://rpc.ankr.com/eth",      // Ankr 公共节点
}
```

**优点**:
- ✅ 无需注册 API Key
- ✅ 自动切换备用节点
- ✅ 更高的可用性

#### ✅ 方案 2: 自动重试机制

程序会自动重试 2 次，增加成功率。

#### ✅ 方案 3: 网络诊断

程序会先测试基础 HTTP 连接，帮助诊断网络问题。

---

## 📊 运行结果

```
=== Web3 Go 进阶示例：带网络诊断和重试 ===

📡 可用节点列表:
  1. https://eth.llamarpc.com
  2. https://cloudflare-eth.com
  3. https://mainnet.infura.io/v3/xxx
  4. https://rpc.ankr.com/eth

🔍 测试网络连接...
⚠️  HTTP 测试失败: Get "https://www.google.com": context deadline exceeded
   可能原因：网络问题或需要代理

尝试连接节点 1/4: https://eth.llamarpc.com
✅ 连接成功！

✅ 当前使用节点: https://eth.llamarpc.com

📊 正在获取区块链信息...
📦 最新区块号: 24053393
🔗 链 ID: 1 (以太坊主网)
💰 Vitalik 地址余额: 7.5397 ETH

============================================================
🎉 恭喜！你已经成功完成以下操作:
  ✅ 连接到以太坊主网
  ✅ 查询最新区块号
  ✅ 查询账户余额
============================================================
```

## 📁 文件说明

### `main.go` (新版本 - 推荐使用)
- 支持多节点自动切换
- 带网络诊断功能
- 自动重试机制
- 查询 Vitalik 余额示例

### `main_basic.go` (原版本 - 备份)
- 只使用单个节点
- 适合理解基础概念

## 🚀 运行程序

```bash
cd web3-go-demo
go run main.go
```

## 💡 关键功能

### 1. 多节点冗余
程序会按顺序尝试连接，直到成功：
```go
for i, nodeURL := range nodes {
    client, err = connectWithRetry(nodeURL, 2)
    if err == nil {
        break  // 连接成功，退出循环
    }
}
```

### 2. 自动重试
每个节点会重试 2 次：
```go
func connectWithRetry(nodeURL string, maxRetries int) (*ethclient.Client, error) {
    for i := 0; i < maxRetries; i++ {
        // 尝试连接
    }
}
```

### 3. 超时控制
每个操作都有超时限制，避免无限等待：
```go
ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
defer cancel()
```

### 4. 余额查询
展示如何查询账户 ETH 余额：
```go
balance, err := client.BalanceAt(ctx, address, nil)
// 转换 Wei 为 ETH (1 ETH = 10^18 Wei)
ethBalance := balance / 10^18
```

---

## 🔍 常见问题

### Q1: 为什么 HTTP 测试失败？
**A**: 你的网络可能需要代理访问 Google。这不影响以太坊节点连接（使用了其他域名）。

### Q2: 如果所有节点都无法连接怎么办？
**A**: 可能原因：
1. 网络完全断开
2. 防火墙阻止了 HTTPS 请求
3. 需要配置代理

### Q3: 公共节点安全吗？
**A**: 
- ✅ 查询数据（读操作）：完全安全
- ⚠️  发送交易（写操作）：需要谨慎，建议使用自己的节点或信任的服务商

### Q4: 公共节点有限制吗？
**A**: 
- 有速率限制，但对学习足够
- 生产环境建议注册专用 API Key

---

## 📚 下一步学习

### 1. 监听合约事件（WebSocket）
```go
// 监听 USDT Transfer 事件
wsClient, _ := ethclient.Dial("wss://eth.llamarpc.com")
```

### 2. 调用智能合约
```go
// 读取 ERC20 代币余额
tokenBalance := contract.BalanceOf(address)
```

### 3. 发送交易（测试网）
```go
// 在 Sepolia 测试网发送交易
auth := bind.NewKeyedTransactor(privateKey)
tx, _ := contract.Transfer(auth, toAddress, amount)
```

---

## 🎯 总结

### 你已经学会了：
- ✅ 连接以太坊节点
- ✅ 处理网络错误和重试
- ✅ 查询区块号和余额
- ✅ 使用多节点提高可用性

### 关键经验：
1. **永远不要只依赖一个节点**
2. **添加重试机制处理网络波动**
3. **使用超时控制避免阻塞**
4. **公共节点适合学习，生产环境需要专用 API**

继续加油！🚀
